This project can take a given csv file and allow the user to search for it in the GUI presented. It can also allow you to view & compare different
requests and how long it took with various algorithms. To run, just download the files and run Main.java and change the path in the Methods.java 
file.
